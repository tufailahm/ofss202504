package threaddemos;

import java.time.Instant;

public class Demo3 {
    public static void main(String[] args) {
//        Runnable task = () -> {
//            String name = Thread.currentThread().getName();
//            for (int i = 0; i < 10; i++) {
//                if(Thread.isInterrupted()) { return; }  // check if thread has received an interrupt signal
//                System.out.println(name+' '+i+' '+ Instant.now()); // perform actions
//                try {
//                    Thread.sleep(10); // pause
//                } catch (InterruptedException ex) {
//                    System.out.println("Thread "+name+" was interrupted while sleeping");
//                    return; // terminate thread if it was interrupted while in timed wait state
//                }
//            };
//
//        };
    }
}
